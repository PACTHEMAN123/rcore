# 忽略 Rust 编译生成的文件夹
**/target/

# Cargo.lock 可以根据项目需求选择性忽略
# 一般建议保留，特别是库项目
# Cargo.lock

# 编译生成的可执行文件（可选，通常在 `target/debug/` 或 `target/release/` 下）
**/*.exe
**/*.out
**/*.so
**/*.dll
**/*.dylib

# IDE 和编辑器的临时文件
**/.idea/
**/.vscode/
*.swp
*.swo

# 系统文件
.DS_Store
Thumbs.db